<!-- $FIELD_NAME_TITLE$ Field -->
'<div class="form-group col-sm-6">' +
    '$FIELD_NAME_TITLE$:<select class="form-control" name="$FIELD_ARRAY_NAME$[' + x + '][$FIELD_NAME$]">'+
        @foreach($$FOREIGN$s as $$FOREIGN$)
            '<option value="{{ $$FOREIGN$->id }}">{{ $$FOREIGN$->name }}' +
        @endforeach
    '</select>' +
'</div>' +